apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: julekalender
  namespace: nada
  labels:
    team: nada
spec:
  image: { { image } }
  port: 3000
  ingresses:
    - "https://jul.labs.nais.io/"
